"""Singular value decomposition, reconstruction, and quality metrics.

This module will own the numerical domain model and pure NumPy operations. It
must remain independent of the web interface so another UI adapter can reuse it.
"""
